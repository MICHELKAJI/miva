import { ServiceSectionDirective } from './service-section.directive';

describe('ServiceSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceSectionDirective();
    expect(directive).toBeTruthy();
  });
});
