import { MenunavigationDirective } from './menunavigation.directive';

describe('MenunavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new MenunavigationDirective();
    expect(directive).toBeTruthy();
  });
});
